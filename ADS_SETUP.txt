ARGAMES ClickAdilla / WP Ad Manager setup

Publisher ID: 453249

Integration behavior:
- The ad-manager loader is placed at the end of each HTML page, after all banner mount elements exist.
- The loader uses data-cfasync="false" and loads the ClickAdilla WP Ad Manager for publisher 453249.
- The web-push service worker RXebVX.js is installed at the site root and registered with scope '/'.

Banner spots (one unique HTML container per spot):
1499081 = 300x250
1499084 = 300x100
1499085 = 300x100
1499086 = 300x100
1499087 = 300x100
1499097 = 300x100

Each spot uses its own exact mount form:
<div data-banner-id="SPOT_ID"></div>

The old EffectiveCPMNetwork scripts are not included.
The homepage has no header ad. Banner spots are distributed through the page, and all six spots are present on category/game pages.

A dedicated ad-test.html page is included to test all six banner mount IDs together.
